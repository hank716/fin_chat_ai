# Secrets — never commit (design_docs.md §29.1)
.env
.env.*
!.env.example
*.key
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Local data / storage (local-first, max 10GB)
# 只忽略根目錄 runtime 資料夾，不要誤殺 backend/storage 這個 Python 套件
/storage/
*.parquet

# Logs
*.log
logs/

# OS / editor
.DS_Store
.idea/
.vscode/
