[pytest]
# backend/ 是 import 根（模組以 reports.* / storage.* / processor.* / config 匯入）。
pythonpath = backend
testpaths = tests
addopts = -q
filterwarnings =
    ignore::FutureWarning
    ignore::DeprecationWarning
